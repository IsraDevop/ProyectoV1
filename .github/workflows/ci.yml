name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: yala_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - "5432:5432"
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin
          cache: maven

      - name: Build and test
        run: mvn clean verify
        env:
          DATABASE_URL: jdbc:postgresql://localhost:5432/yala_test
          DB_USER: postgres
          DB_PASS: postgres
          JWT_SECRET: test-secret-key-for-ci-must-be-32-chars
          CCI_ENCRYPTION_KEY: test-cci-encryption-key-for-ci-32-chars
          STRIPE_SECRET_KEY: sk_test_placeholder
          STRIPE_WEBHOOK_SECRET: whsec_placeholder
          AWS_REGION: us-east-1
          AWS_S3_BUCKET: yala-ci-placeholder
          AWS_S3_PUBLIC_URL_BASE: https://yala-ci-placeholder.s3.us-east-1.amazonaws.com
          RESEND_API_KEY: placeholder

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: target/surefire-reports/
